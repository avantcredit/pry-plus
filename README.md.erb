pry-plus
========

Get up and going with a good set of pry tools right away.

Based on [RKing's pry-plus](https://github.com/rking/pry-plus). Updated with support for ruby 2.0.

<%
  require 'yaml'
  list = YAML.load_file 'in.yml'
%>

Dependent Gems
--------------

<% list.each do |name, info| %>
### [<%= name %>](<%= info['url'] %>)

<%= info['desc'] %>

<% if info['use cases'] %>
  <% info['use cases'].each do |e| %>
- <%= e %>
  <% end %>
<% end %>

<% end %>
